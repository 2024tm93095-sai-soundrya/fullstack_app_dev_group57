School Equipment Lending Portal

This is a **Full-Stack web application** that allows students and administrators to manage school equipment lending.  
The project is implemented using **Java Spring Boot (backend)** and **React (frontend)** with an **H2 in-memory database*
